package geom.algorithms;

import java.util.List;
import geom.math.Vector;
import geom.structures.dcel.Dcel;
import geom.structures.extra.*;

/**
 * Algoritmo para calcular la posición de cámaras de seguridad en una subdivisión,
 * de tal forma que todos las caras de dicha subdivisión se encuentren vigilados.
 * 
 */
public class SurveillanceCameras {

  /**
   * Calcula las posiciones de las cámaras de seguridad.
   * 
   * @param  dcel La subdivisón con los cuartos a vigilar.
   * @return      La lista de posición de las cámaras.
   */
  public static List<Vector> calculateSurveillanceCameras (Dcel dcel) {
    return null;
  }

  public static Camera[] getSurveillanceCameras(Dcel dcel) {
    return new Camera[0];
  }
}
